import {NextPage} from "next";
import RecommendationsSlider from "@/ui/RecommendationsSlider/RecommendationsSlider";

const SiteMap :NextPage= () => {
    return (
        <div>
            <RecommendationsSlider />
        </div>
    );
};

export default SiteMap;